const config = {
    canvasWidthMultiplier: .96,
    canvasHeightMultiplier: .88,
    distanceEpsilon: 20,
    pointRadius: 10,
    lineWidth: 7,
}

export const colorsConfig = {
    point: "rgb(248 250 252)", //"bg-slate-50"
    selectedPoint: "rgb(251 191 36)", // "bg-amber-50"
    edge: "rgb(148 163 184)", // "bg-amber-50"
    selectedEdge: "rgb(251 191 36)", // "bg-slate-400"
}

export default config;
